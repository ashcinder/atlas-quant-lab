[package]
name = "atlas-backtest-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
atlas-zk-core = { path = "../../core" }
risc0-zkvm = { version = "=3.0.6", default-features = false, features = ["std"] }
