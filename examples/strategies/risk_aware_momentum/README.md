# Risk-aware Momentum

This example uses only closed bars supplied by the runner. Package the directory contents—not the directory itself—as `risk_aware_momentum.qstrategy`. AI may review the baseline targets, but the deterministic risk gate always runs afterward.
