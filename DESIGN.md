# 策略实验室设计记录

**范围**：仅 `策略实验室 / 工作流`（`GuidedWorkflow`）surface；不定义或替换全站样式。

## 结构与信息层级

- 先用工作流名称、起步模板和 AI 数量说明当前起点，再以可跳转的六步目录组织画布：观察市场、决定买卖、分配仓位、控制风险、模拟成交、复盘与记录。
- 每一步用编号、图标、交易语言的问题和规则积木表达。积木按交易顺序由连线和箭头串联；缺少的模板步骤明确标出。
- AI 是阶段内可插入的独立积木，不作为独立技术画布。其权限状态可见，并始终标注为“待接入执行”。审计阶段说明 AI 不能修改审计证据。
- 桌面为主画布加右侧粘性设置栏。基础字段直接呈现，节点 ID、模型、异常处理、历史档案等放在“高级设置”中；策略规则跳到独立规则页面。

## 视觉语言

- 沿用 Atlas 深墨蓝壳：页面 `#111a27`，画布 `#152030`，普通卡片 `#1c2a3d`，AI 卡片 `#252f48`；以低对比蓝灰边线区分层级。
- 文字以近白 `#e8eef8` 为主，辅助信息为蓝灰 `#acbdd2`；淡蓝 `#b7ccff` 表示选择、焦点与主操作。错误使用浅粉色，验证契约使用青绿色。
- 继承产品无衬线字体 `var(--sans, sans-serif)`；标题约 23–32px，阶段标题 16px，正文/控件 12–14px，帮助文字与元信息 11px。标题略收紧字距，数字使用等宽数字。
- 间距以 8px 节奏展开：外层桌面横向 28px、画布内 24px、卡片 12–16px；卡片 12px 圆角，主画布 16px 圆角。宽屏侧栏 310px，1600px 以上为 350px。

## 交互与可访问性

- 点击规则或 AI 积木即选中并打开对应设置；阶段目录滚动到相应步骤。模板以下拉面板载入，未保存草稿需要确认替换。
- 卡片有悬停和选中状态；所有表单、按钮与折叠项都有淡蓝 `:focus-visible` 外框。支持键盘操作、ARIA 标签与 `prefers-reduced-motion`。
- 保存账户设置、模板和高级字段默认折叠，以保留“先搭流程、再深入配置”的路径。
- 在 820px 以下，画布与设置栏改为单列；选择积木后自动滚到设置，设置栏提供“返回策略画布”。移动端缩小边距、阶段编号和卡片内距，隐藏 AI 数量和桌面模式提示。

## 能力边界

- 当前画布可编辑并保存工作流、做结构校验、配置 AI 职责与权限；AI 编排未接入执行，也不参与回测。
- 回测规则由独立规则编辑页配置。画布不执行实盘交易；独立交易账户页面按用户新要求提供人工确认接口，画布的有效结构不等于策略已验证。
- 历史策略档案、ZKP 证明与 SDK 是更多工具入口；历史归档上传仍退役；新的代码模块支持将 .py 文件导入本地编辑器。

## 代码策略模块（2026-09-08）

- 实验室顶部并列两种开发方式，切换保留页面草稿。
- 左侧 Python 编辑器，右侧 AI 助手；深墨蓝底、等宽语法高亮、薄边线、薄荷色操作按钮。
- 名称、导入、下载、检查置于单行工具栏；窄屏自动换行，助手移至编辑器下方。
- AI 接入真实本机 Ollama 服务；未配置时明确说明。生成内容先预览，覆盖新编辑需确认，可撤销替换。
- 当前只提供语法检查；没有代码回测或实盘运行。代码草稿保留在页面，下载保存到本地文件。

## 多语言扩展（2026-09-08）
工具栏语言选择器提供14种语言；切换保留各语言代码草稿，AI会话按语言切换重置并取消旧请求。文件扩展名自动识别，跨语言导入保护目标草稿；下载使用所选语言的扩展名。非Python检查禁用并显示未接入编译/回测说明。

## 交易账户（2026-09-09）
继承全站深色主题、细边线与淡蓝操作色。模式 Operate：顶部绑定状态，中部限价委托与账户查询左右分栏，底部订单核实；820px以下单列。实盘环境显式标红，预览固定参数，精确文字确认后提交；未知结果置顶持续提示。A股显示待接入，不显示虚构账户。

## 全站主题与信息层级（2026-09-09）
用户要求切换主题协调、减少信息冗杂及小字。统一html data-theme与语义颜色；顶部提供浅色、深色、跟随系统并本机保存，所有工作区和Canvas同步。保留深墨蓝视觉体系，浅色采用淡灰底与白色内容面。正文/表单提高可读字号，导航使用单层标签。研究页取消不相关的回测状态条，重复阶段介绍收起，项目状态精简；风险、未知成交、账户环境和确认仍显式显示。

## 策略运行与自动交易账本（2026-09-10）

策略工作台增加可运行策略库，仍沿用现有工作台页签，不替换开发画布。交易账户改为操作概览：顶部先展示按原币拆分的净值和收益，中部创建及管理单标的策略实例，底部保留人工限价单和账户连接。市场与环境是独立筛选维度，平台模拟、交易所测试和实盘始终明确标注。

运行卡片展示版本、标的、净值、收益率、最新信号、错误原因及生命周期操作。窄屏下筛选、表单和卡片改为单列，关键状态和人工确认入口不折叠。资产账本增加独立“自动交易账本”页签，按账户、市场、环境和策略筛选服务端成交；成交通过深链接返回对应运行实例，旧版手工账户、OCR、定投和账本数据流保持不变。

## 交易账户整页工作台（2026-09-12，替代前次局部布局）

模式 Operate。用户否定卡片与表单堆叠的页面，要求整页设计由代理决定。当前采用稳定账户导航与连续主内容：桌面左栏205px、间隔38px，页面最大1480px；概览、策略运行、委托记录按任务切换。700px以下导航改为三等分横排，交易所账户双列，主内容单列。

全站语义深浅主题及强调色保留。标题采用现有Avenir/PingFang显示字体30px，分区20–29px，正文13–15px；金额使用等宽数字。主区域采用连续分隔线而非同尺寸卡片。概览账户与运行摘要共用一块16px圆角内容面，策略资产以原币条目陈列；策略改为可展开的连续列表，详情显示曲线、持仓、信号和成交。

新建运行、人工下单仅通过明确入口打开Base UI Dialog，复用已有焦点管理、遮罩与Esc退出。表单保留草稿与预览，预览期间锁定交易所切换。成功提交后进入委托记录，未知结果常驻页面提醒；刷新不重开已关闭表单，不覆盖编辑中的策略版本。深链接及异步响应按hash快照和请求序号防止迟到数据覆盖。

连接状态、环境与交易权限独立；配置指引和其他市场按需展开，币安/欧易切换保留按钮焦点并清除旧余额。账户资产与策略净值不合并，运行概况标明筛选范围并可重置。真实资金提示保留，数据缺失显示空状态，无虚构净值曲线或账户总额。
