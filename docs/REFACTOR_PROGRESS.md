# 重构进度与验证边界

更新：2026-09-07。此前 UI 检查点为 `55e3980`；本轮实现为 `1907430`，均已推送 GitHub `QuantJudge` 分支。

## 本轮交付

### 连贯的操作流程

- 实验室与组合配置首次访问后保留在内存；切换主导航不会丢失编辑状态，不把源码、提示词和凭证写入 localStorage。
- 规则 / 工作流替换前确认；编辑中保存不会把后来修改误标为已保存。
- 切换 Agent 清空旧凭证、包绑定、证明与数据集；迟到的请求不能恢复旧会话资料。
- 项目请求更新来源项目，不因晚返回而跳回之前的项目。
- 组件错误隔离在当前工作区，并提供重载入口。

### 状态与展示

- 顶部新增“系统状态”，区分后端在线、证明验证器就绪、链连接和尚未接入的能力。
- 启动部分失败不再阻断所有目录，提供重新连接入口；网络、超时、Pydantic 校验错误均有可读提示。
- 请求没有自动写入重试；超时提示后台任务可能仍在运行，避免重复提交。
- 切换标的 / 周期清除旧图表和交易结果，迟到回测不会覆盖新标的。
- 交易表使用结果的实际计价币种并保留分钟时间；风险指标采用中文说明，组合增加相关性矩阵，未定义值显示“—”。

### 后端正确性

- 内置策略参数统一校验；DCA 间隔为零等非法输入返回 422。
- 修复 MACD 周期关系与马丁 RSI 入场参数未真正生效的问题。
- 组合先完成卖出再按资金比例买入，消除成分排列顺序导致的调仓偏差。
- 校验重复标的、有限数值和日期；规范化组合代码，避免显式权重匹配失败。
- 常量序列相关性使用 null；结果明确记录 valuation_currency，不改变原有会计计算。
- 后端测试的数据库、缓存、证明与密钥均隔离到临时目录。

### 私有部署基础

新增非 root 容器、只读根文件系统、单 worker、回环监听、持久化数据卷、同源代理、健康检查、依赖版本固定、CI 与可重复的容器冒烟测试。详见 [部署指南](DEPLOYMENT.md)。没有启动或修改 Supervisor，没有进行真实交易、收费或对外部署。

## 本地与远端验收（2026-09-07）

| 检查 | 结果 |
| --- | --- |
| 前端生产构建、ESLint | 通过 |
| 前端 Vitest | 11 个文件，37 项通过 |
| 后端 Pytest | 68 项通过；1 项第三方弃用警告 |
| 本轮修改的 Python 文件 Ruff | 通过；不是全仓 lint 结论 |
| Compose 配置解析、Git 差异空白检查 | 通过 |
| 浏览器工作区切换 | 实验室草稿在切换至组合并返回后保留 |
| 桌面与移动端界面 | 实际截图检查；390 px 下页面无横向溢出，状态面板可滚动 |
| GitHub Linux 前端 / 后端验证 | 全部通过，与本轮实现提交 `1907430` 对应 |
| GitHub Linux 镜像构建、容器运行与持久化冒烟 | 通过：真实镜像、路由、离线回测、重建后的数据库与密钥一致性 |

浏览器检查没有保存临时验收草稿，没有向真实账户或 Supervisor 写入。截图保留在本机 `output/playwright/`，不上传私有工作空间截图。

远端验收：[GitHub Actions #34077168667](https://github.com/ashcinder/atlas-quant-lab/actions/runs/34077168667)，前端、后端、容器三个 job 均为 success。该记录不等于公网部署、生产数据恢复演练或容器 ZKP 验收。

## 尚未完成：本轮交付不等于全平台已生产就绪

本轮没有实现任意 Python 策略执行、真实 AI 推理、TEE、真实支付、多租户安全架构，也没有扩大固定 SMA 的 ZKP 覆盖范围。基础容器未捆绑 Linux zkVM 验证器。

本机 Docker 守护进程未运行，本机部署仍待启动 Docker 后验证；GitHub Linux 环境已通过真实容器构建与重建持久化检查，但尚未进行生产数据备份恢复演练。后端未修改文件仍有既存 lint 问题，因此仅将改动范围 lint 通过与完整测试通过作为本轮结论，不声称全仓无缺陷。
